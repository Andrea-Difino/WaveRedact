[package]
name = "waveredact_core"
version = "0.1.0"
edition = "2024"

[lib]
name = "waveredact_core"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.29.2", features = ["extension-module"] }

